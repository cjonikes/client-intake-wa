export interface UserCredentialsProps {
    email: string;
    password: string;
};

export interface UserInfo {
    userId:  Number,
    usertype: string,
    username: string,
    lastLogin: string,
};